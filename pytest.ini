[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    unit: pure unit tests (no I/O to real data files)
    integration: requires real data files and trained model artifacts
